extends layout.pug

block content
  form(action=continueURL)
    h3 So what do you want to call the quiz?
    input.form-control(name="quizTitle" required)
    input.btn.btn-primary(type="submit" value="Create quiz")